<nav class="navbar navbar-default navbar-fixed" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Delishus", root_path, :class=>"navbar-brand"%>

      </a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "About Us", about_path%>
        </li>
        <li>
          <%= link_to "Contact Us", contact_us_path%>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <li>
            <%= link_to new_dish_path do %>
              <span class="glyphicon glyphicon-cutlery"></span> Add a Dish
            <% end %>
          </li>
        </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Hello <%= current_user.firstName  %>!<b class="caret"></b></a>
              <ul class="dropdown-menu dropdown-menu-right">
                <li>
                  <%= link_to edit_user_registration_path, align: "right" do %>
                    Account Settings <span class="glyphicon glyphicon-cog"></span>
                  <% end %>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to destroy_user_session_path, align: "right", method: :delete do %>
                    Log Out <span class="glyphicon glyphicon-log-out"></span>
                  <% end %>
                </li>
              </ul>
          <% elsif !user_signed_in? %>
            <li><%=link_to "Log In", new_user_session_path, align: "right" %></li>
          <% end %>
        </li><!-- /dropdown -->
      </ul>
    </div><!-- .navbar-collapse -->
  </div><!-- .container -->
</nav>